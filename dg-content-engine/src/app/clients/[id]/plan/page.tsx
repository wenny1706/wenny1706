import { ComingSoon } from '@/components/ComingSoon';
export default function Page() {
  return <ComingSoon stage="PLAN" phase={2}
    what="Content pillars plus the monthly content map, with one-click push of a planned item into the CREATE pipeline." />;
}
